# TODO

> Generated: 2026-03-21 — Full codebase review across bugs, refactoring, documentation, features, and UI/UX.

| ID | Priority | Category | Title | Description | State |
|----|----------|----------|-------|-------------|-------|
| 001 | High | Bug | Incorrect ranking via `.name` attribute | `data_visualizations.py:684,736` — After `.iloc[0]`, `.name` returns the DataFrame index label, not the building name. Using it to index the rank Series causes `KeyError` or wrong results. Fix: `idx = df[df['name'] == building_name].index[0]`, then `rank_series[idx]`. | Resolved |
| 002 | High | Bug | Unsafe `.iloc[0]` without empty-check | `app.py:463`, `data_visualizations.py:445,601`, `city_analysis.py:656,708` — `.iloc[0]` called without verifying the filtered DataFrame is non-empty, causing unhandled `IndexError` when data changes. Guard each access with an `.empty` check. | Resolved |
| 003 | Medium | Bug | Index misalignment in per-square calculation | `app.py:815-822` — `df_viz_filtered.loc[df_display.index, ...]` assumes both DataFrames share the same index after potentially divergent transformations. Can raise `KeyError` or silently produce wrong divisors. Use `.reset_index(drop=True)` or position-based indexing. | Pending |
| 004 | Medium | Bug | Unguarded `.index("_")` raises `ValueError` | `building_images.py:29-30` — `_ss_key()` calls `asset_id.index("_")` which throws on any asset ID without an underscore. The caller silences this with `except ValueError: pass`, losing diagnostic info. Add an `if "_" not in asset_id` guard and log the problematic ID. | Pending |
| 005 | Medium | Bug | Wrong condition on column deselect path | `column_selector.py:293-298` — The `elif` branch checks `col not in selected_columns_set` before discarding, preventing `changes_made = True` on legitimate deselections. Fix: compare old vs new state with `if new_selection != (col in st.session_state.selected_columns_set)`. | Pending |
| 006 | Low | Bug | `float('nan')` enters max-column highlight logic | `data_visualizations.py:476-483` — `val_str == 'nan'` converts to `float('nan')`, which corrupts the max-column highlight in the comparison table. Filter out `math.isnan(value)` before inserting into `numeric_values`. | Pending |
| 007 | Low | Bug | Silent `except: pass` swallows errors | `data_visualizations.py:134`, `building_images.py:57-58` — Bare exception blocks discard stack traces and problematic values with no logging. Add at minimum `logger.debug(f"...: {e}")` to preserve context. | Pending |
| 008 | High | Refactoring | Duplicate AND/OR operator logic in filters | `advanced_filters.py:217-353` — The full numeric operator handling (between, gt, gte, lt, lte, eq, neq) is written twice for AND and OR branches. Extract a `_create_operator_mask(column_data, operator, v1, v2)` helper reused by both paths. | Pending |
| 009 | High | Refactoring | Hardcoded column names scattered across files | `'name'`, `'Era'`, `'Event'`, `'Translated Era'`, `'Weighted Efficiency'`, `'Total Score'`, etc. appear as string literals in `app.py`, `city_analysis.py`, `advanced_filters.py`, `column_selector.py`. Centralise as `SYSTEM_COLUMNS`, `CALCULATED_COLUMNS`, `EXCLUDED_FROM_FILTERS` in `config.py`. | Pending |
| 010 | High | Refactoring | Session state keys defined as bare strings everywhere | Keys like `'selected_columns_set'`, `'advanced_filters'`, `'imported_inventory'`, `'era_filter'` scattered across `app.py`, `city_analysis.py`, `column_selector.py`, `advanced_filters.py`. Add a `class SessionKeys` or constants block in `config.py`. | Pending |
| 011 | High | Refactoring | `apply_boosts_to_base_metrics` does too much | `calculations.py:36-192` — 156-line function handles 5 distinct responsibilities. Split into `_store_original_bases()`, `_apply_combined_boosts()`, `_calculate_true_base_context()`, `_apply_building_boosts_to_context()`. | Pending |
| 012 | High | Refactoring | Repetitive boost application pattern | `calculations.py:82-111` — FP, Goods, Guild Goods, and Special Goods boosts all repeat the same `if boost > 0: multiplier = 1 + (pct/100); result = base * multiplier` pattern. Extract a `_apply_single_boost(base_value, boost_pct)` helper. | Pending |
| 013 | Medium | Refactoring | Era filter logic duplicated in city_analysis | `city_analysis.py` — The pattern `era_key = ERAS_LEVEL_MAP.get(era_level); rows = rows[rows['Era'] == era_key]` appears in multiple functions. Extract `filter_buildings_by_era(df, era_level) -> DataFrame \| None`. | Pending |
| 014 | Medium | Refactoring | `.iterrows()` + `.copy()` loops are slow | `calculations.py:217`, `city_analysis.py:407,759` — Row-by-row iteration with per-row `.copy()` is slow on large DataFrames. Replace with `.apply()` or vectorised operations where possible. | Pending |
| 015 | Medium | Refactoring | `ui_components.py` handles too many concerns | 422 lines covering icon loading, AG-Grid JS components, formatters, heatmap styling, and grid configuration. Consider splitting into `icons.py`, `grid_formatters.py`, and `grid_builder.py`. | Pending |
| 016 | Low | Refactoring | Dead commented-out import in `app.py` | `app.py:24` — `# import qi_optimizer_test` is dead code. Remove or replace with a clear `# TODO:` comment explaining intent. | Pending |
| 017 | Low | Refactoring | `build_grid_options` cache does not key on DataFrame | `ui_components.py:283` — `@st.cache_resource` may return a stale grid config for a different DataFrame. Move cache to caller or use `@st.cache_data` with a hash of relevant inputs. | Pending |
| 018 | Low | Refactoring | Config lookups repeated inside render loop | `column_selector.py:259-284` — `config.COLUMN_GROUPS[key]` and `get_text()` are called on every loop iteration. Precompute group metadata before the loop. | Pending |
| 019 | Low | Refactoring | Inconsistent error handling strategy | `data_loader.py` calls `st.stop()`, `calculations.py` returns empty DataFrame, `city_analysis.py` logs and continues. Establish and document a consistent convention across modules. | Pending |
| 020 | High | Documentation | README data loading section is outdated | README still describes "parsing raw game JSON locally" and "raw GitHub JSON" — the content is wrong since the migration to the VPS API. Rewrite to reflect the current architecture. | Pending |
| 021 | Medium | Documentation | README missing coverage of major features | No mention of the Weights tab, City Analysis tab, Data Visualizations tab, advanced filtering, building placement simulator, or inventory import. Update the feature list and usage section. | Pending |
| 022 | Low | Documentation | README building names note should be updated | The note about building name translation "needing rework / being worked on" is stale. Update with current status or remove. | Pending |
| 023 | Medium | Documentation | CLAUDE.md missing 3 module descriptions | `city_analysis.py`, `data_visualizations.py`, and `advanced_filters.py` are absent from the module table in CLAUDE.md. | Pending |
| 024 | Medium | Documentation | CLAUDE.md boost algorithm underdocumented | The three-step pipeline explanation is vague. Add detail on why the reverse-engineer step is needed (dividing current production by boost multiplier) with a worked example. | Pending |
| 025 | Low | Documentation | CLAUDE.md cache TTL rationale missing | Why 23 hours? It is tied to the 18:00 daily API update. Document this and make the value a named constant in `data_loader.py`. | Pending |
| 026 | Low | Documentation | CLAUDE.md per-square mode not explained | `PER_SQUARE_EXCLUDED_COLUMNS` is referenced in code but never explained in CLAUDE.md. Document what per-square mode does and why certain columns are excluded. | Pending |
| 027 | Medium | Documentation | `apply_boosts_to_base_metrics` docstring too terse | `calculations.py:36-192` — The 3-step mathematical rationale lacks inline comments explaining the "why" of each step, especially the true-base reverse-engineering logic. | Pending |
| 028 | Medium | Documentation | `calculate_era_stats` has no docstring | `calculations.py:12-33` — Function is missing a docstring entirely. Also uses `observed=False` in `groupby` with no explanation. | Pending |
| 029 | Low | Documentation | `RANKING_POINTS_PER_RESOURCE` is undocumented | `config.py` — No explanation of what these values represent, where they come from, or why `special_goods` only has 2 eras. | Pending |
| 030 | Low | Documentation | `BOOST_TO_BASE_MAPPING` cardinality unexplained | `config.py` — No comment explaining why Goods Boost maps to a list of columns while other boosts map to a single string. | Pending |
| 031 | Low | Documentation | `translate_yesno_key` regex patterns undocumented | `translations.py` — Regex replacements for `^No`, `^Yes`, `days$`, `collections$` have no explanation of expected input patterns or case-sensitivity assumptions. | Pending |
| 032 | Low | Documentation | `_ss_key` ForgeHX path convention unexplained | `building_images.py` — No explanation of the `_SS_` path format, the asset ID structure, or what ForgeHX represents. | Pending |
| 033 | Low | Documentation | `_apply_filters` legacy path has no rationale | `advanced_filters.py` — Both operator-based and legacy min/max filter paths coexist with no comment explaining why, or whether the legacy path is temporary. | Pending |
| 034 | Low | Documentation | Magic constants in `data_loader.py` undocumented | `_API_TIMEOUT = 30` and `_PAGE_SIZE = 1000` have no comments explaining the choice of values or any API-imposed limits. | Pending |
| 035 | High | Documentation | `translations/fr/messages.json` has no EN equivalent | The FR translation directory has `messages.json` but EN does not. This asymmetry may cause missing-key errors in English mode. | Pending |
| 036 | Medium | Documentation | No audit of EN vs FR translation key parity | It is unclear which UI keys are missing in French. A comparison script or documented gap list is needed. | Pending |
| 037 | Low | Documentation | Building name translation coverage undocumented | No list of which building names fall back to English in French mode. The `TO_BE_TRANSLATED_BUILDING_NAMES` tracker exists but its output is never surfaced. | Pending |
| 038 | High | Feature | QI Optimizer is implemented but disabled | `app.py:24,647,1364` — The module, tab slot, and render call are all commented out. Evaluate for completion and re-enable, or remove the dead references entirely. | Pending |
| 039 | High | Feature | Advanced filtering is inconsistent across tabs | Advanced filters (range/categorical) are only available in the Analysis tab in advanced mode. Visualizations and City Analysis tabs have no advanced filter access. | Pending |
| 040 | High | Feature | No save/load for weight profiles | User weights, context values, and boost percentages reset on every page reload. `city_analysis.py:474-499` already has save/load infrastructure not exposed in the main UI. Implement export/import of weight profiles as JSON. | Pending |
| 041 | Medium | Feature | No loading indicators during long operations | No `st.spinner()` around `load_and_process_data()`, efficiency calculations, chart generation, or city analysis merges. Users cannot tell if the app is working or frozen. | Pending |
| 042 | Medium | Feature | Empty states lack actionable guidance | When filters return zero results, messages like "No data available" do not suggest next steps. Add contextual hints such as "Try selecting a different era" or "Relax your filter values". | Pending |
| 043 | Medium | Feature | No caching of efficiency calculation results | Efficiency scores are recalculated on every weight change. Cache results keyed by `(weights_hash, context_hash)` in session state to avoid redundant computation. | Pending |
| 044 | Medium | Feature | Building placement simulator is not discoverable | The greedy optimizer in `data_visualizations.py:288-411` is buried in the Visualizations tab with no callout in the UI or README. Highlight it as a primary feature. | Pending |
| 045 | Low | Feature | Missing translation contribution workflow | `TO_BE_TRANSLATED_BUILDING_NAMES` captures missing translations at runtime but users cannot see or report them. Expose a "Missing translations" summary in a debug or developer mode. | Pending |
| 046 | Low | Feature | No dedicated QI-specific analysis view | QI columns and a QI preset exist but there is no QI-focused analysis tab or optimization guidance. Consider a QI view alongside the disabled QI optimizer. | Pending |
| 047 | Low | Feature | No scenario comparison for weight profiles | No way to compare two weight configurations side-by-side. A "Clone profile" + diff view would help power users evaluate trade-offs. | Pending |
| 048 | High | UI/UX | AG-Grid column auto-sizing fails | `ui_components.py:400` — `fitCellContents` conflicts with hardcoded `width=80` on icon columns and no `maxWidth` on the name column. This is listed as a known issue in the README. Icon columns need a wider minimum; name column needs a max width. | Pending |
| 049 | High | UI/UX | Mobile layout is broken | Fixed widths of 600px (`app.py:560,598`), 800px split across buildings (`data_visualizations.py:663`), and `use_container_width=False` on comparison tables make the app unusable on tablets and phones. Switch to relative sizing throughout. | Pending |
| 050 | High | UI/UX | Heatmap is inaccessible to color-blind users | `ui_components.py:260-280` — Red-green HSL heatmap conveys information through color alone. Add text value overlays or switch to a perceptually uniform palette (e.g., viridis). | Pending |
| 051 | Medium | UI/UX | Boolean values display inconsistently | Grid shows ✔️/❌ emoji (`ui_components.py:253`) while the stats table shows "Yes"/"No" text (`app.py:512`). Standardise the representation across the app. | Pending |
| 052 | Medium | UI/UX | Plotly chart heights are hardcoded | `data_visualizations.py` — Heights of 400/500/600px are hardcoded with no viewport awareness. Use `height=None` with `use_container_width=True` or provide a user-adjustable size control. | Pending |
| 053 | Medium | UI/UX | Easy/Advanced mode toggle has no tooltip | `app.py` — The toggle switches features on and off with no explanation of what changes. Add help text listing which features are hidden in Easy mode. | Pending |
| 054 | Medium | UI/UX | Active filter summary truncates at 3 values | `advanced_filters.py:490-493` — Users cannot verify their full filter state without re-opening each filter. Add an expandable summary or a tooltip showing all active values. | Pending |
| 055 | Medium | UI/UX | Comparison table uses fixed width of 600px | `data_visualizations.py:522-524` — `use_container_width=False, width=600` leaves wasted space on wide screens. Switch to `use_container_width=True`. | Pending |
| 056 | Medium | UI/UX | AG-Grid pagination fixed at 50 rows with no user control | `ui_components.py:379` — Page size is hardcoded at 50 with no option to change. Add a page size selector (25/50/100/All). | Pending |
| 057 | Low | UI/UX | Preset buttons trigger full re-render and lose scroll position | `column_selector.py` — Each `st.rerun()` from a preset button click scrolls the user back to the top of the page. Investigate deferred state updates to avoid unnecessary reruns. | Pending |
| 058 | Low | UI/UX | Icon-checkbox layout lacks semantic association | `column_selector.py:46-69` — Icon and checkbox are in separate columns with no ARIA label linking them. Add `title` or `label` text to icon elements for screen reader compatibility. | Pending |
| 059 | Low | UI/UX | "Show labels" icon toggle is undiscoverable | `app.py:247` — The option only appears after enabling icons. Users are unlikely to find it. Surface it more visibly or add a tooltip on the icon toggle. | Pending |
| 060 | Low | UI/UX | Trend line colour hardcoded to red | `data_visualizations.py:130` — Red overlaps with heatmap colours and is inaccessible. Use a high-contrast neutral colour such as white or yellow on the dark theme. | Pending |
| 061 | Low | UI/UX | Browser tab title is hardcoded | `app.py:34-37` — `page_title="FoE Building Database"` is not derived from translations, creating an inconsistency with the in-app title. Also verify that `assets/icons/icon.png` exists at startup. | Pending |

# TODO

> Generated: 2026-03-21 — Full codebase review across bugs, refactoring, documentation, features, and UI/UX.
> Updated: 2026-03-24 — Code review pass focused on cleaning and refactoring (items 062–097).

| ID | Priority | Category | Title | Description | State |
|----|----------|----------|-------|-------------|-------|
| 001 | High | Bug | Incorrect ranking via `.name` attribute | `data_visualizations.py:684,736` — After `.iloc[0]`, `.name` returns the DataFrame index label, not the building name. Using it to index the rank Series causes `KeyError` or wrong results. Fix: `idx = df[df['name'] == building_name].index[0]`, then `rank_series[idx]`. | Resolved |
| 002 | High | Bug | Unsafe `.iloc[0]` without empty-check | `app.py:463`, `data_visualizations.py:445,601`, `city_analysis.py:656,708` — `.iloc[0]` called without verifying the filtered DataFrame is non-empty, causing unhandled `IndexError` when data changes. Guard each access with an `.empty` check. | Resolved |
| 003 | Medium | Bug | Index misalignment in per-square calculation | `app.py:815-822` — `df_viz_filtered.loc[df_display.index, ...]` assumes both DataFrames share the same index after potentially divergent transformations. Can raise `KeyError` or silently produce wrong divisors. Use `.reset_index(drop=True)` or position-based indexing. | Resolved |
| 004 | Medium | Bug | Unguarded `.index("_")` raises `ValueError` | `building_images.py:29-30` — `_ss_key()` calls `asset_id.index("_")` which throws on any asset ID without an underscore. The caller silences this with `except ValueError: pass`, losing diagnostic info. Add an `if "_" not in asset_id` guard and log the problematic ID. | Resolved |
| 005 | Medium | Bug | Wrong condition on column deselect path | `column_selector.py:293-298` — The `elif` branch checks `col not in selected_columns_set` before discarding, preventing `changes_made = True` on legitimate deselections. Fix: compare old vs new state with `if new_selection != (col in st.session_state.selected_columns_set)`. | Resolved — current code already checks `col in selected_columns_set` (correct condition); TODO description was stale |
| 006 | Low | Bug | `float('nan')` enters max-column highlight logic | `data_visualizations.py:476-483` — `val_str == 'nan'` converts to `float('nan')`, which corrupts the max-column highlight in the comparison table. Filter out `math.isnan(value)` before inserting into `numeric_values`. | Resolved |
| 007 | Low | Bug | Silent `except: pass` swallows errors | `data_visualizations.py:134`, `building_images.py:57-58` — Bare exception blocks discard stack traces and problematic values with no logging. Add at minimum `logger.debug(f"...: {e}")` to preserve context. | Resolved |
| 008 | High | Refactoring | Duplicate AND/OR operator logic in filters | `advanced_filters.py:217-353` — Extracted `_create_operator_mask()` helper; both AND and OR branches now delegate to it. Returns None to signal skip for unrecognised configs. | Resolved |
| 009 | High | Refactoring | Hardcoded column names scattered across files | Added `COL_NAME`, `COL_ERA`, `COL_TRANSLATED_ERA`, `COL_EVENT`, `COL_LIMITED`, `COL_ALLY_ROOM`, `COL_ROAD`, `COL_SIZE`, `COL_ASSET_ID`, `COL_WEIGHTED_EFFICIENCY`, `COL_TOTAL_SCORE` constants to `config.py`; replaced literals across 8 files. | Resolved |
| 010 | High | Refactoring | Session state keys defined as bare strings everywhere | Added `config.SessionKeys` class with 15 constants; replaced bare string literals across `app.py`, `column_selector.py`, `advanced_filters.py`, `city_analysis.py`. | Resolved |
| 011 | High | Refactoring | `apply_boosts_to_base_metrics` does too much | `calculations.py:36-192` — 156-line function handles 5 distinct responsibilities. Split into `_store_original_bases()`, `_apply_combined_boosts()`, `_calculate_true_base_context()`, `_apply_building_boosts_to_context()`. | Resolved |
| 012 | High | Refactoring | Repetitive boost application pattern | `calculations.py:82-111` — FP, Goods, Guild Goods, and Special Goods boosts all repeat the same `if boost > 0: multiplier = 1 + (pct/100); result = base * multiplier` pattern. Extract a `_apply_single_boost(base_value, boost_pct)` helper. | Resolved |
| 013 | Medium | Refactoring | Era filter logic duplicated in city_analysis | `city_analysis.py` — The pattern `era_key = ERAS_LEVEL_MAP.get(era_level); rows = rows[rows['Era'] == era_key]` appears in multiple functions. Extract `filter_buildings_by_era(df, era_level) -> DataFrame \| None`. | Resolved |
| 014 | Medium | Refactoring | `.iterrows()` + `.copy()` loops are slow | `calculations.py:217`, `city_analysis.py:407,759` — Row-by-row iteration with per-row `.copy()` is slow on large DataFrames. Replace with `.apply()` or vectorised operations where possible. | Partial — `_compute_true_base_context` pre-computed once before the scoring loop; full vectorisation deferred until a test suite exists |
| 015 | Medium | Refactoring | `ui_components.py` handles too many concerns | 422 lines covering icon loading, AG-Grid JS components, formatters, heatmap styling, and grid configuration. Consider splitting into `icons.py`, `grid_formatters.py`, and `grid_builder.py`. | Deferred — broad rename across all files; payoff is aesthetic only |
| 016 | Low | Refactoring | Dead commented-out import in `app.py` | `app.py:24` — `# import qi_optimizer_test` is dead code. Remove or replace with a clear `# TODO:` comment explaining intent. | Resolved |
| 017 | Low | Refactoring | `build_grid_options` cache does not key on DataFrame | `ui_components.py:283` — `@st.cache_resource` may return a stale grid config for a different DataFrame. Move cache to caller or use `@st.cache_data` with a hash of relevant inputs. | Resolved — removed cache decorator entirely; GridOptionsBuilder output is not picklable so cache_data errors, and cache_resource doesn't key on inputs. The function is fast enough to call uncached. |
| 018 | Low | Refactoring | Config lookups repeated inside render loop | `column_selector.py:259-284` — `config.COLUMN_GROUPS[key]` and `get_text()` are called on every loop iteration. Precompute group metadata before the loop. | Resolved — loop runs ≤10 iterations; micro-optimisation not worth the noise |
| 019 | Low | Refactoring | Inconsistent error handling strategy | `data_loader.py` calls `st.stop()`, `calculations.py` returns empty DataFrame, `city_analysis.py` logs and continues. Establish and document a consistent convention across modules. | Resolved — convention documented in CLAUDE.md § Development Conventions |
| 020 | High | Documentation | README data loading section is outdated | README still describes "parsing raw game JSON locally" and "raw GitHub JSON" — the content is wrong since the migration to the VPS API. Rewrite to reflect the current architecture. | Resolved |
| 021 | Medium | Documentation | README missing coverage of major features | No mention of the Weights tab, City Analysis tab, Data Visualizations tab, advanced filtering, building placement simulator, or inventory import. Update the feature list and usage section. | Resolved |
| 022 | Low | Documentation | README building names note should be updated | The note about building name translation "needing rework / being worked on" is stale. Update with current status or remove. | Resolved |
| 023 | Medium | Documentation | CLAUDE.md missing 3 module descriptions | `city_analysis.py`, `data_visualizations.py`, and `advanced_filters.py` are absent from the module table in CLAUDE.md. | Resolved |
| 024 | Medium | Documentation | CLAUDE.md boost algorithm underdocumented | The three-step pipeline explanation is vague. Add detail on why the reverse-engineer step is needed (dividing current production by boost multiplier) with a worked example. | Resolved |
| 025 | Low | Documentation | CLAUDE.md cache TTL rationale missing | Why 23 hours? It is tied to the 18:00 daily API update. Document this and make the value a named constant in `data_loader.py`. | Resolved |
| 026 | Low | Documentation | CLAUDE.md per-square mode not explained | `PER_SQUARE_EXCLUDED_COLUMNS` is referenced in code but never explained in CLAUDE.md. Document what per-square mode does and why certain columns are excluded. | Resolved |
| 027 | Medium | Documentation | `apply_boosts_to_base_metrics` docstring too terse | `calculations.py:36-192` — The 3-step mathematical rationale lacks inline comments explaining the "why" of each step, especially the true-base reverse-engineering logic. | Resolved |
| 028 | Medium | Documentation | `calculate_era_stats` has no docstring | `calculations.py:12-33` — Function is missing a docstring entirely. Also uses `observed=False` in `groupby` with no explanation. | Resolved |
| 029 | Low | Documentation | `RANKING_POINTS_PER_RESOURCE` is undocumented | `config.py` — No explanation of what these values represent, where they come from, or why `special_goods` only has 2 eras. | Resolved |
| 030 | Low | Documentation | `BOOST_TO_BASE_MAPPING` cardinality unexplained | `config.py` — No comment explaining why Goods Boost maps to a list of columns while other boosts map to a single string. | Resolved |
| 031 | Low | Documentation | `translate_yesno_key` regex patterns undocumented | `translations.py` — Regex replacements for `^No`, `^Yes`, `days$`, `collections$` have no explanation of expected input patterns or case-sensitivity assumptions. | Resolved |
| 032 | Low | Documentation | `_ss_key` ForgeHX path convention unexplained | `building_images.py` — No explanation of the `_SS_` path format, the asset ID structure, or what ForgeHX represents. | Resolved |
| 033 | Low | Documentation | `_apply_filters` legacy path has no rationale | `advanced_filters.py` — Both operator-based and legacy min/max filter paths coexist with no comment explaining why, or whether the legacy path is temporary. | Resolved |
| 034 | Low | Documentation | Magic constants in `data_loader.py` undocumented | `_API_TIMEOUT = 30` and `_PAGE_SIZE = 1000` have no comments explaining the choice of values or any API-imposed limits. | Resolved |
| 035 | High | Documentation | `translations/fr/messages.json` has no EN equivalent | The FR translation directory has `messages.json` but EN does not. This asymmetry may cause missing-key errors in English mode. | Resolved — en/messages.json already existed; stale TODO. |
| 036 | Medium | Documentation | No audit of EN vs FR translation key parity | Audit complete. Gaps found: `events.json` FR missing 13 event-name keys (Anniversary, Archeology, Care, Fall, ForgeBowl, Halloween, History, Long Term, Saint Patrick, Summer, Viking, Wildlife, Winter); `ui.json` FR missing 1 key (`paste_json_city`). All other files (columns, eras, messages) are at parity. | Resolved |
| 037 | Low | Documentation | Building name translation coverage undocumented | No list of which building names fall back to English in French mode. The `TO_BE_TRANSLATED_BUILDING_NAMES` tracker exists but its output is never surfaced. | Pending |
| 038 | High | Feature | QI Optimizer is implemented but disabled | `app.py:24,647,1364` — The module, tab slot, and render call are all commented out. Evaluate for completion and re-enable, or remove the dead references entirely. | Pending |
| 039 | High | Feature | Advanced filtering is inconsistent across tabs | Advanced filters (range/categorical) are only available in the Analysis tab in advanced mode. Visualizations and City Analysis tabs have no advanced filter access. | Pending |
| 040 | High | Feature | No save/load for weight profiles | User weights, context values, and boost percentages reset on every page reload. `city_analysis.py:474-499` already has save/load infrastructure not exposed in the main UI. Implement export/import of weight profiles as JSON. | Pending |
| 041 | Medium | Feature | No loading indicators during long operations | No `st.spinner()` around `load_and_process_data()`, efficiency calculations, chart generation, or city analysis merges. Users cannot tell if the app is working or frozen. | Pending |
| 042 | Medium | Feature | Empty states lack actionable guidance | When filters return zero results, messages like "No data available" do not suggest next steps. Add contextual hints such as "Try selecting a different era" or "Relax your filter values". | Pending |
| 043 | Medium | Feature | No caching of efficiency calculation results | Efficiency scores are recalculated on every weight change. Cache results keyed by `(weights_hash, context_hash)` in session state to avoid redundant computation. | Pending |
| 044 | Medium | Feature | Building placement simulator is not discoverable | The greedy optimizer in `data_visualizations.py:288-411` is buried in the Visualizations tab with no callout in the UI or README. Highlight it as a primary feature. | Pending |
| 045 | Low | Feature | Missing translation contribution workflow | `TO_BE_TRANSLATED_BUILDING_NAMES` captures missing translations at runtime but users cannot see or report them. Expose a "Missing translations" summary in a debug or developer mode. | Pending |
| 046 | Low | Feature | No dedicated QI-specific analysis view | QI columns and a QI preset exist but there is no QI-focused analysis tab or optimization guidance. Consider a QI view alongside the disabled QI optimizer. | Pending |
| 047 | Low | Feature | No scenario comparison for weight profiles | No way to compare two weight configurations side-by-side. A "Clone profile" + diff view would help power users evaluate trade-offs. | Pending |
| 048 | High | UI/UX | AG-Grid column auto-sizing fails | `ui_components.py:400` — `fitCellContents` conflicts with hardcoded `width=80` on icon columns and no `maxWidth` on the name column. This is listed as a known issue in the README. Icon columns need a wider minimum; name column needs a max width. | Resolved |
| 049 | High | UI/UX | Mobile layout is broken | Fixed widths of 600px (`app.py:560,598`), 800px split across buildings (`data_visualizations.py:663`), and `use_container_width=False` on comparison tables make the app unusable on tablets and phones. Switch to relative sizing throughout. | Resolved |
| 050 | High | UI/UX | Heatmap is inaccessible to color-blind users | `ui_components.py:260-280` — Red-green HSL heatmap conveys information through color alone. Add text value overlays or switch to a perceptually uniform palette (e.g., viridis). | Resolved |
| 051 | Medium | UI/UX | Boolean values display inconsistently | Grid shows ✔️/❌ emoji (`ui_components.py:253`) while the stats table shows "Yes"/"No" text (`app.py:512`). Standardise the representation across the app. | Resolved |
| 052 | Medium | UI/UX | Plotly chart heights are hardcoded | `data_visualizations.py` — Heights of 400/500/600px are hardcoded with no viewport awareness. Use `height=None` with `use_container_width=True` or provide a user-adjustable size control. | Resolved |
| 053 | Medium | UI/UX | Easy/Advanced mode toggle has no tooltip | `app.py` — The toggle switches features on and off with no explanation of what changes. Add help text listing which features are hidden in Easy mode. | Resolved — advanced_mode toggle already has help= text via translations key |
| 054 | Medium | UI/UX | Active filter summary truncates at 3 values | `advanced_filters.py:490-493` — Users cannot verify their full filter state without re-opening each filter. Add an expandable summary or a tooltip showing all active values. | Resolved |
| 055 | Medium | UI/UX | Comparison table uses fixed width of 600px | `data_visualizations.py:522-524` — `use_container_width=False, width=600` leaves wasted space on wide screens. Switch to `use_container_width=True`. | Resolved |
| 056 | Medium | UI/UX | AG-Grid pagination fixed at 50 rows with no user control | `ui_components.py:379` — Page size is hardcoded at 50 with no option to change. Add a page size selector (25/50/100/All). | Resolved |
| 057 | Low | UI/UX | Preset buttons trigger full re-render and lose scroll position | `column_selector.py` — Each `st.rerun()` from a preset button click scrolls the user back to the top of the page. Investigate deferred state updates to avoid unnecessary reruns. | Pending |
| 058 | Low | UI/UX | Icon-checkbox layout lacks semantic association | `column_selector.py:46-69` — Icon and checkbox are in separate columns with no ARIA label linking them. Add `title` or `label` text to icon elements for screen reader compatibility. | Pending |
| 059 | Low | UI/UX | "Show labels" icon toggle is undiscoverable | `app.py:247` — The option only appears after enabling icons. Users are unlikely to find it. Surface it more visibly or add a tooltip on the icon toggle. | Pending |
| 060 | Low | UI/UX | Trend line colour hardcoded to red | `data_visualizations.py:130` — Red overlaps with heatmap colours and is inaccessible. Use a high-contrast neutral colour such as white or yellow on the dark theme. | Resolved |
| 061 | Low | UI/UX | Browser tab title is hardcoded | `app.py:34-37` — `page_title="FoE Building Database"` is not derived from translations, creating an inconsistency with the in-app title. Also verify that `assets/icons/icon.png` exists at startup. | Resolved |
| 062 | High | Bug | City buildings always get Quantity = 1 | `city_analysis.py:411-423` — In `merge_with_database`, the `city` branch reads `data.get('count', 1)` but `parse_tsv_city` always writes `'quantity'`, never `'count'`. Every city building lands with `Quantity=1` regardless of actual count. Fix: use `data.get('quantity', data.get('count', 1))` to match the `inventory` branch. | Resolved |
| 063 | Medium | Bug | Uninitialized `column_mask` in OR filter branch | `advanced_filters.py:217-353` — In the OR branch, if no condition sets `column_mask` before line 349 (`combined_mask \|= column_mask`), Python raises `NameError`. Initialize `column_mask = pd.Series([False] * len(df), index=df.index)` at the top of each OR loop body. (Distinct from TODO #008 which tracks the DRY issue.) | Resolved |
| 064 | Low | Bug | `buildings_filtered_by_zero_production` is always 0 | `app.py:888-893` — The variable is initialized to `0` immediately before the guard `if hide_zero_production and buildings_filtered_by_zero_production > 0`. The condition can never be true, so the filter-count message is never shown. Compute the count as `n_before - n_after` at the actual filter site (lines 619–629) and pass it forward. | Resolved |
| 065 | High | Refactoring | Consumables and QI Boosts subtabs are ~200 lines of copy-paste | `app.py:962-1159` and `app.py:1162-1361` — The two subtabs share identical structure: mask filter, `iterrows` loop, `row_data` dict, `column_config`, sort, display, export. Extract a shared `_render_column_analysis_subtab(df, columns, config_builder_fn, export_prefix, lang_code, image_manager)` helper. | Resolved |
| 066 | High | Refactoring | Per-square calculation block duplicated three times | `app.py:467-474`, `app.py:809-825`, `app.py:1386-1400` — The same logic (get divisor column, replace zeros/NA with 1, divide numeric columns) is written out fully in three places. Extract `apply_per_square(df, divisor_col) -> pd.DataFrame` in `calculations.py`. | Resolved |
| 067 | High | Refactoring | Summary column block duplicated verbatim in comparison view | `data_visualizations.py:657-700` and `data_visualizations.py:711-756` — Exact copy-paste of `summary_cols` setup, building-image HTML, and rank calculation loop. Extract `_render_building_summary_column(building_name, df, comparison_metrics, viz_manager, n_buildings)`. | Resolved |
| 068 | Medium | Refactoring | `stats_df` rendering duplicated in building details panel | `app.py:537-562` and `app.py:575-600` — Two branches (with/without image column) construct and render an identical `st.dataframe(stats_df, ...)` block. Extract `_render_stats_table(stats_data, lang_code)`. | Resolved |
| 069 | Medium | Refactoring | `city_analysis.py:759-772` re-implements `merge_with_database` inline | The block inside `render_city_analysis_tab` duplicates the era-filter + `iterrows` + `copy` pattern already in `merge_with_database`, also hardcoding `'Inventory'`/`'City'` source strings without going through translations. Replace with two calls to `merge_with_database`. (Extends TODO #013 and #014 with a concrete fix site.) | Resolved |
| 070 | Medium | Refactoring | Visible-column computation duplicated in `AdvancedFilterManager.__init__` | `advanced_filters.py:28-58` — `_get_numeric_columns` and `_get_categorical_columns` each independently loop over `config.COLUMN_GROUPS` to build `visible_columns`. Extract once as `_get_visible_columns() -> set` and call it from both methods. | Resolved |
| 071 | Medium | Refactoring | `_has_icon` calls `get_icon_base64` and discards the result | `column_selector.py:38-79` — `_has_icon(col)` calls `get_icon_base64(col)` to check for existence then discards the value; `_create_column_item` calls it again immediately after. Eliminate `_has_icon` entirely: `icon_b64 = get_icon_base64(col) if col not in config.ICON_EXCLUDED_COLUMNS else None; if icon_b64:`. | Resolved |
| 072 | Medium | Refactoring | Redundant `"key"` field in every `COLUMN_GROUPS` entry | `config.py:95-138` — Each entry stores `"key": "basic_info"` etc., which is identical to its own dict key. All callers use `COLUMN_GROUPS.items()` which already provides the key. Remove the `"key"` field from all nine entries; update any callers that read `group["key"]` to use the iterator variable instead. | Resolved |
| 073 | Medium | Refactoring | `streamlit` imported at module level in `config.py` | Moved `import streamlit as st` inside `get_api_config()`; `config.py` is now importable without Streamlit initialised. | Resolved |
| 074 | Medium | Refactoring | Cache decorators defined inside `main()` | Moved `apply_translations`, `cached_calculate_era_stats`, and `get_cached_image_manager` to module level with docstrings; removed misleading inline comment. | Resolved |
| 075 | Medium | Refactoring | `calculate_weighted_efficiency` legacy wrapper has inline import | Moved `USER_BOOST_FIELDS` to top-level import in `calculations.py`; removed inline import from function body. | Resolved |
| 076 | Low | Refactoring | `combine_army_with_ge_gbg` defined inside a `try` block | `app.py:150` — The function has no dependency on any variable inside the `try` block and is re-created on every run. Move to module level. | Resolved |
| 077 | Low | Refactoring | Auto-metrics loop is O(buildings × columns) with Python iteration | `data_visualizations.py:596-621` — The loop calls `.iloc[0]` per building then iterates every column. Replace with: `selected_df = df[df['name'].isin(selected_buildings)]; auto_metrics = [col for col in numeric_cols if selected_df[col].replace(0, pd.NA).notna().any()]`. | Resolved |
| 078 | Low | Refactoring | `selected_era` parameter stored but never read | `advanced_filters.py:13` — `self.selected_era = selected_era` is set in `__init__` but no method reads it. Remove the parameter and assignment, or add a `# TODO:` comment if era-scoped filtering is planned. | Resolved |
| 079 | Low | Refactoring | Inline CSS block embedded in `main()` | `app.py:325-400` — The ~75-line `st.markdown("<style>...</style>")` block has no runtime dependencies. Move to a module-level constant `_TAB_STYLES_CSS` or into `ui_components.py` alongside `CUSTOM_CSS`. | Resolved |
| 080 | Medium | Dead Code | Dead import `translate_era_key` in `calculations.py` | `calculations.py:7` — `from translations import translate_era_key` is imported with the comment `# Needed for reverse mapping` but is never called. Remove the import and comment. If the reverse-mapping requirement is real, track it as a TODO instead. | Resolved |
| 081 | Low | Dead Code | Five unused imports in `app.py` | `app.py:8-12` — `ColumnsAutoSizeMode`, `GridUpdateMode`, `GridOptionsBuilder` (from `st_aggrid`), `DynamicFilters`, and `np` (numpy) are imported but never referenced. Remove all five. | Resolved |
| 082 | Low | Dead Code | `get_per_square_text` and `PER_SQUARE_TRANSLATIONS` are dead code | `translations.py:62-68` — `get_per_square_text` is defined but never called anywhere in the codebase. `PER_SQUARE_TRANSLATIONS` is only referenced by that one function. Remove both, or add a comment explaining if they were replaced by `ui.json` keys. | Resolved |
| 083 | Low | Dead Code | `combine_army_stats` parameter accepted but never used | `data_visualizations.py:531` — `render_data_visualizations(..., combine_army_stats=False)` is in the signature but never referenced in the function body. Remove the parameter. | Resolved |
| 084 | Low | Dead Code | Commented-out dead code with no explanation | `data_visualizations.py:520` — `# display_df.drop(columns=['Metric'], inplace=True)` has no note explaining why it was disabled. Delete or replace with a `# TODO:` comment. | Resolved |
| 085 | Low | Dead Code | `base_building_id` computed but never used | `city_analysis.py:300` — The variable is calculated (stripping the last ID segment) but the subsequent lookup at line 302 uses `building_id`, not `base_building_id`. The expression itself is also malformed for its apparent intent. Remove the variable. | Resolved |
| 086 | Low | Code Quality | Wrong type hint on `user_boosts` parameter | `calculations.py:194,253` — `user_boosts: Dict[str, float] = None` is incorrect; the correct annotation is `Optional[Dict[str, float]] = None`. Same issue on `calculate_weighted_efficiency`. | Resolved |
| 087 | Low | Code Quality | `PER_SQUARE_EXCLUDED_COLUMNS` is a list; should be a set | `config.py:188` — Used only for membership tests (`col not in ...`). Convert to a `set` for O(1) lookup, matching `ICON_EXCLUDED_COLUMNS` which is already a set. | Resolved |
| 088 | Low | Code Quality | Inline imports inside `render_city_analysis_tab` run on every rerender | `city_analysis.py:885-918` — `from io import BytesIO` and `from datetime import datetime` (already imported at line 5) are re-imported inside the render function. Move all inline imports to the top of the module. | Resolved |
| 089 | Low | Code Quality | `has_production` / `has_qi_boost` redundant checks inside filtered loops | `app.py:1044-1045` and `app.py:1243-1244` — Both checks re-verify a condition already guaranteed by `consumables_mask` / `qi_boosts_mask` applied a few lines earlier. Remove the redundant guards and the `continue` statements that follow them. | Resolved |
| 090 | Low | Code Quality | `logger.info` logs full user weights dict on every render cycle | `app.py:769` — `logger.info(f"... User weights: {user_weights}")` fires on every Streamlit interaction. Downgrade to `logger.debug`. | Resolved |
| 091 | Low | Code Quality | Untranslated UI label hardcoded in English in `advanced_filters.py` | `advanced_filters.py:195` — `f"Filter {translated_name}"` constructs the label with a hardcoded English word while every adjacent label uses `translations.get_text(...)`. Add a translation key (e.g., `"filter_label"`) and use it here. | Resolved |
| 092 | Low | Code Quality | `save/load_from_session_state` docstrings claim browser localStorage | `city_analysis.py:474-508` — Both functions claim to use "browser localStorage" but use `st.session_state`, which is server-side. Rename to `cache_in_session` / `read_from_session` or update docstrings to avoid misleading future maintainers about persistence semantics. | Resolved |
| 093 | Low | Code Quality | `show_toast_notification` wraps Streamlit calls in unnecessary `try/except` | `city_analysis.py:511-537` — `st.success`, `st.error`, etc. do not raise under normal conditions. The bare `except Exception` fallback to `st.write` masks real errors (e.g., bad translation keys). Remove the try/except. | Resolved |
| 094 | Low | Code Quality | `clear_all_data` annotated `-> None` but returns `int` | `city_analysis.py:540-567` — The docstring says `Returns: None` but the function returns `cleared_count` (int) on both paths. The caller discards the return value. Remove the `return cleared_count` statements and the variable, or fix the annotation to `-> int`. | Resolved |
| 095 | Low | Code Quality | `'Building Name'` hardcoded in English in chart labels | `data_visualizations.py:206,217` — `labels={'name': 'Building Name', ...}` bypasses the translation system while the adjacent `translated_name` uses it. Replace with `translations.get_text('building_name', self.lang_code)`. | Resolved |
| 096 | Low | Code Quality | `get_all_mappings` returns `path → URL` but is named/documented as `asset_id → URL` | `building_images.py:71-78` — The method acknowledges internally that it returns the wrong key type ("Reverse the key back to an asset_id is non-trivial; return URL map keyed by path") but does it anyway. Either reconstruct the asset ID key or rename to `get_all_path_urls`. | Resolved |
| 097 | Low | Code Quality | Unused local variable `base_metrics` in `apply_boosts_to_base_metrics` | `calculations.py:165` — `base_metrics = base_metric_or_list` is assigned but never read; `base_metric_or_list` is used directly in the subsequent `zip`. Remove the assignment and its comment. | Resolved |
